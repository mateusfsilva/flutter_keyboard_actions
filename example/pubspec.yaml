name: example
description: Sample project using keyboard actions.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0<3.0.0"

dependencies:
  flutter:
    sdk: flutter

  keyboard_actions: 
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
